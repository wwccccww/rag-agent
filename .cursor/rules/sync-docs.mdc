---
description: 每次添加/修改/删除功能时同步更新 README.md 并提供测试方案
alwaysApply: true
---

# 文档同步规则

每次对项目进行功能变更时，必须同步执行以下操作：

## 必须同步更新 README.md 的场景

- 新增 API 端点或参数
- 修改现有功能行为
- 删除功能或端点
- 新增配置项（`.env` 变量）
- 新增依赖包

## README.md 更新内容要求

1. **功能说明**：描述功能做什么、为什么这么设计
2. **API 变更**：更新「主要 API」表格
3. **配置项**：更新「配置项」表格（如有新增 `.env` 变量）
4. **测试方案**：提供具体可执行的测试步骤

## 测试方案格式

每个新功能必须提供：

```
**测试步骤：**
1. 具体操作步骤
2. 运行的命令（如有）

**预期输出：**（日志、UI 变化、或命令输出示例）
```

## 不需要更新文档的场景

- Bug 修复（不改变对外行为）
- 代码重构（不改变功能）
- CSS 样式微调
